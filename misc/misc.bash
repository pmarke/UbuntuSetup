#!/bin/bash

# This file is used to install misc programs

# HTOP - interactive process viewer. Used to see cpu utilization
sudo apt install htop

# Install slack
sudo snap install slack --classic

# System monitor
sudo apt-get install indicator-multiload

# 